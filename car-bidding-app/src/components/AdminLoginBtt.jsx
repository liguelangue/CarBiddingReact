// src/components/LoginButton.js
// Up to date -- wxm
import React from 'react';

function AdminLoginButton({ onClick }) {
    return <button onClick={onClick}>Admin Login</button>;
}

export default AdminLoginButton;
